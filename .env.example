NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

DATABASE_URL=postgresql://user:password@localhost:5432/task_manager_dev?schema=public

JWT_SECRET=
JWT_EXPIRES_IN=1h

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Part 3 — how many hours ahead the deadline-reminder cron looks (default 24)
DEADLINE_REMINDER_WINDOW_HOURS=24
